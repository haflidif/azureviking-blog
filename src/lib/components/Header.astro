---
import { SITE } from '@/config';
import { basePath } from '@/lib/utils/basePath';
---

<header class="flex items-center justify-between p-4">
  <a href={basePath('/')} class="text-2xl font-bold uppercase tracking-wider">
    {SITE.title}
  </a>
  <nav class="flex gap-4">
    <a href={basePath('/post')} class="hover:text-gray-400">Blog</a>
    <a href={basePath('/about')} class="hover:text-gray-400">About</a>
  </nav>
</header>
